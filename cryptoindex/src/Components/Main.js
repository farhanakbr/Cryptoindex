import React from 'react'

export default function Main(props) {

    const {quote, generateRandomQuote} = props;
    return (
        <div className="App">

           {/* <button 
                onClick{() => {generateRandomQuote(quotes)} } type="submit">
                <i class="fas fa-mouse">Get Quote</i>    
           </button> */}

        </div>
    )
}
